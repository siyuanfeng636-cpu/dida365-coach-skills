interface:
  display_name: "Dida Coach"
  short_description: "滴答清单任务教练，负责拆解、时间盒、复盘与闭环支持"
  default_prompt: "Use $dida-coach to turn my goal or task into a Dida-backed plan with timeboxes and review prompts."

dependencies:
  tools:
    - type: "mcp"
      value: "dida365"
      description: "滴答清单 MCP，用于读取和写入任务"
      transport: "streamable_http"
      url: "https://mcp.dida365.com"

policy:
  allow_implicit_invocation: true
