interface:
  display_name: "Dida Coach"
  short_description: "基于 dida-cli 的滴答执行层 + 本地生产力管控层，负责拆解、时间盒、承诺、复盘与闭环支持"
  default_prompt: "Use $dida-coach to log into Dida with dida-cli, then build a Dida-backed productivity system with planning, timeboxes, commitments, reviews, and follow-through."

policy:
  allow_implicit_invocation: true
